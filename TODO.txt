
TODO List
    Symbol
        Add jackson deserialization

    Semantics
        Fix spawn's semantics

    iCode
        serialize and deserialize iCode objects
        Add all static quads to the code seg before main gets called
        Remove size calculation from iCode and move that into tCode
        Remaining iCode to add:
            templates
            spawn
    
    tCode    
        Use an interface with tCode manager to allow different types of tCode generation 

    Compiler
        Create a new error listener for the SymbolTableVisitor to store syntax errors into 
            compiler's exception list

    Update README.md