

public void main() {
    bool isDefault = true;

    int test = (10 > (2 * 2)) ? 3 : 5;

    //string str = isDefault ? "Hello" : "world";
    //string str = isDefault ? "Hello" : 10;
    //string str = 5 ? "Hello" : "world";
}
